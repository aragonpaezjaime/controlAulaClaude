# ============================================
# VARIABLES DE ENTORNO - CONTROL DE AULA
# ============================================

# Entorno de ejecución (development, production)
NODE_ENV=production

# Puerto del servidor (Render lo asigna automáticamente)
PORT=3000

# MongoDB Atlas - URI de conexión
# Obtén esta URI desde MongoDB Atlas > Connect > Drivers
# Ejemplo: mongodb+srv://usuario:password@cluster.mongodb.net/nombreDB
MONGODB_URI=mongodb+srv://tu_usuario:tu_password@tu_cluster.mongodb.net/control_aula?retryWrites=true&w=majority

# ============================================
# NOTAS IMPORTANTES PARA DEPLOYMENT
# ============================================

# 1. NO subas el archivo .env real a GitHub
# 2. Configura MONGODB_URI en Render desde el dashboard
# 3. Asegúrate de que MongoDB Atlas permita conexiones desde cualquier IP (0.0.0.0/0)
# 4. El PORT lo asigna Render automáticamente, no lo cambies
